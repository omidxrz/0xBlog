# 0z.ci

Personal blog — articles, CVEs, archive, hall of fame.

Static HTML/CSS/JS, no build step. Served by GitHub Pages at [0z.ci](https://0z.ci/).

Edit the `.html` files directly and push to `main` to deploy.
